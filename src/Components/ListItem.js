import React from 'react';
import List from './List';

const ListItem = (props) => {
	return <div>{props.value}</div>;
};

export default ListItem;

// movieProp={movieProp} characterProp={characterProp}
